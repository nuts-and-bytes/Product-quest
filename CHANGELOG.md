# 更新日志

记录每次迭代**改了什么**，以及**为什么这么改**——后者更重要，因为它决定了下一次该往哪走。

版本号遵循 `主版本.次版本`：次版本 = 新玩法 / 新章节 / 体验改动；主版本 = 学习主线的结构性变化。

---

## v1.7 — 数据法医 & 打字音效重制

> 这一版的主题是：**同样的内容，换一种"读"的方式。**

### 🔍 新玩法：数据法医

**问题**：37 关全都是"读一段话 → 点一个文字按钮"。疲劳感不来自内容差，来自**输入通道只有一种**。

**做法**：不新增内容，只把选项从文字按钮换成图上的可点元素——玩家直接在数据上下刀。

关键洞察是 `pick(c)` 只吃 `{t, s, fb, lesson, term}` 这几个字段，它不关心这个 `c` 是从文字按钮来的，还是从图上点来的。所以三级判定、信任扣减、翻车时光倒流、评分、术语解锁**全部零改动复用**——引擎只多了一层渲染。这也是本项目的一条设计原则：**最好的机制不生产内容，它让已经写好的内容被读第二遍。**

落地三关，按难度阶梯排布（ch1–ch5 新手章节一律不动，避免劝退）：

| 关卡 | 形态 | 怎么玩 |
|---|---|---|
| 第 26 关 · 北极星与虚荣指标 | `dash` 仪表盘 | 四张指标卡（含一张"已识破"参照卡，只看不可点），点出真正的北极星指标 |
| 第 27 关 · 海盗的漏斗 | `funnel` 漏斗 | 条宽 = 人数、落差块宽 = 流失人数，点你要修的那一段落差 |
| 第 33 关 · 付费墙 | `wall` 白板 | 三张便利贴划定免费边界。原文字三选一**就地替换**，反馈文案逐字复用 |

第 33 关没有新增第三道题，而是替换原题——因为它后面紧跟一道三选一，新增会考同一个点；更重要的是这题的杀招恰恰在于"锁钱额度"必须**可点**：玩家得在看起来最值钱的核心功能上，主动忍住手。

**修复**：参照卡（`.fref`）没有 `data-i`，此前会被 `querySelectorAll('.fcard')` 选中，点击它会 `pick(undefined)` 直接抛 TypeError。改用 `.fcard:not(.fref)`。

### 🔉 打字音效重制

**问题**："对话出字的音效有点吵"，手机上尤其明显。

**诊断**（四个叠加的成因，不是单一的"音量大"）：

1. **触发密度过高**——每 20ms 出一字、每 3 字响一声 ≈ **16.7 次/秒**。人耳在这个重复频率下会把离散脉冲听成一段连续嗡鸣，那已经不是"打字感"了。
2. **方波 + 高频**——`square` 波在 880–1100Hz，其奇次谐波正好落在人耳最敏感的 2–5kHz 区间，手机扬声器还会把这一段再抬一层。
3. **瞬时起音**——`gain.setValueAtTime(vol)` 是一个 DC 跳变，每一声开头都带一个爆音 click。**这才是"刺耳"的真正来源，比音量数值更要命。**
4. **音高抖动 ±220Hz**——听起来像警报器，不像键盘。

**改法**（只重写 `SFX.type()`，其余音效一个字没动——它们一次只响一声，不存在同样的问题）：

| 指标 | 改前 | 改后 |
|---|---|---|
| 发声次数（54 字台词） | 18 | **10**（−44%） |
| 每秒发声 | 16.7 | **9.3** |
| 波形 | `square` | `triangle` |
| 基频 | 880–1100Hz | **293–321Hz**（降约 1.5 个八度） |
| 音高抖动 | ±220Hz | **±18Hz** |
| 低通滤波 | 无 | **1400Hz**（再削一层高次谐波） |
| 起音 | 瞬时跳变 → 爆音 | **0 → 4ms 淡入 → 指数衰减** |

外加：**标点和空格不发声**。句读处自然留白，耳朵有地方喘气——这是所有像素游戏对话音效的共同做法。

### 🔇 静音状态现在会被记住

静音按钮一直存在，但状态不持久化——玩家关掉音效，一刷新它又回来了，等于没关掉。现在存 `localStorage`，跨会话保留，图标同步。

### 契约遵守

零依赖 / 零构建 / 双击 `index.html` 即玩 / 零美术资产 / 零音频文件（全部 WebAudio 实时合成）/ 向后兼容（`forensic` 是可选字段，老关卡一行不用改）。

---

## v1.6 — 术语速查系统

全站英文缩写自动变成可点击链接，一点弹出全称 + 中文释义。

---

> 本文件自 v1.6 起开始记录。更早的版本没有留下变更记录，不予追述。
